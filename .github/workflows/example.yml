name: example
on: [push]
jobs:
  example-job
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
      
      
      

# Questions
# bulk comment?
# is yaml === yml?


